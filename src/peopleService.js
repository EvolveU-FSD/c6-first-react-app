
export function findPersonById(id) {
    // do something and return a person
}

export function updatePerson(person) {
    // call the server and update the person object
}

export function deletePerson(id) {
    // call the server to delete a person
}